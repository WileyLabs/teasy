package com.wiley.autotest.selenium.elements.upgrade.v3;

import org.openqa.selenium.WebDriver;

/**
 * Fluent waiter for any cases where you need to perform actions with WebDriver
 */
public class WebDriverFluentWait extends AnyFluentWait<WebDriver> {

    public WebDriverFluentWait(WebDriver driver) {
        super(driver);
    }

}
